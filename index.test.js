// index.test.js
const index = require('./index'); // Ajusta la ruta si es necesario

describe('Pruebas de index.js', () => {
    it('debería inicializar el módulo index', () => {
        // Ejemplo: Verifica que index esté definido
        expect(index).toBeDefined();
    });

    // Agrega más pruebas según sea necesario
});