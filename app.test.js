// app.test.js
const app = require('./app');

describe('Pruebas de app.js', () => {
    it('debería realizar alguna prueba de inicialización', () => {
        // Ejemplo: Verifica que la aplicación se inicie correctamente
        expect(app).toBeDefined();
    });

    // Agrega más pruebas según la funcionalidad de app.js
});